name: 'Parse Prevention Output'
description: 'Parse the output of the prevention action'
inputs:
  github-token:
    description: 'GitHub token'
    required: true
  iac-files-dir:
    description: 'Directory where IaC files are stored'
    required: false
    default: 'iac_files'
runs:
  using: 'node20'
  main: 'index.js'
